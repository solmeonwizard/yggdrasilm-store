backend:
  name: git-gateway
  branch: main

media_folder: "images"
public_folder: "/images"

collections:
  - name: "products"
    label: "პროდუქტები"
    folder: "data" # აქ შეინახება თითოეული პროდუქტი ცალკე ფაილად
    create: true
    extension: "json"
    format: "json"
    slug: "{{title}}"
    fields:
      - {label: "დასახელება", name: "title", widget: "string"}
      - {label: "ფასი", name: "price", widget: "string"}
      - {label: "აღწერა", name: "description", widget: "text"}
      - {label: "სურათი", name: "image", widget: "image"}